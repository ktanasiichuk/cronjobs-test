apiVersion: batch/v1  
kind: CronJob  
metadata:  
  name: kyrilltest1-sfgnb
  labels:  
    groupName: KyrillTest1ArgoTest
  namespace: scheduledtask-test
spec:  
  schedule: "0 2 * * *"
  suspend: true
  jobTemplate:  
    spec:
      template:
        metadata:
          labels:
            groupName: KyrillTest1
        spec:  
          containers:  
          - name: backup  
            image: busybox:1.35  
            command:  
            - /bin/sh  
            - -c  
            - "echo 'Running backup task 1' && sleep 30"  
          restartPolicy: OnFailure
